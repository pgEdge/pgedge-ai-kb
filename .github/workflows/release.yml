name: Release

on:
    push:
        tags:
            - 'v*'
    workflow_dispatch:
        inputs:
            simulate_tag:
                description: 'Simulate a tag push for end-to-end branch testing (e.g. v0.0.1-test1). Blank = binary snapshot only. Backup-to-S3 and repo-push stay disabled — only real v* tag pushes reach S3.'
                required: false
                default: ''
            force_push:
                description: 'On a real v* tag push: publish artifacts from cells that succeeded even if some failed. Ignored on simulate_tag runs (push is always gated off there).'
                required: false
                type: boolean
                default: false

permissions:
    contents: write
    packages: write

concurrency:
    group: release-${{ github.ref_name }}
    cancel-in-progress: false

jobs:
    # =========================================================================
    # Binary builds (unchanged from prior version)
    # =========================================================================

    build-amd64:
        name: Build x86_64
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
              with:
                  fetch-depth: 0

            - name: Fetch all tags
              run: git fetch --force --tags

            - name: Set up Go
              uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6
              with:
                  go-version: '1.25'
                  cache: true

            - name: Run Go unit tests
              run: go test -v ./cmd/... ./internal/...

            - name: Run GoReleaser (amd64 only)
              uses: goreleaser/goreleaser-action@1a80836c5c9d9e5755a25cb59ec6f45a3b5f41a8 # v7
              with:
                  distribution: goreleaser
                  version: latest
                  args: release --clean --config .goreleaser-amd64.yaml ${{ github.event_name == 'workflow_dispatch' && github.ref_type != 'tag' && '--snapshot' || '' }}
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  # On a real v* tag push, ref_name is the tag. On
                  # simulate_tag dispatch, use the simulated tag so the
                  # produced tarball filename matches what packaging expects.
                  GORELEASER_CURRENT_TAG: ${{ github.event.inputs.simulate_tag || github.ref_name }}

            - name: Upload amd64 artifacts
              uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
              with:
                  name: release-artifacts-amd64
                  path: |
                      dist/*.tar.gz
                      dist/*.zip
                  retention-days: 1

    build-arm64:
        name: Build ARM64
        runs-on: ubuntu-24.04-arm
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
              with:
                  fetch-depth: 0

            - name: Fetch all tags
              run: git fetch --force --tags

            - name: Set up Go
              uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6
              with:
                  go-version: '1.25'
                  cache: true

            - name: Run Go unit tests
              run: go test -v ./cmd/... ./internal/...

            - name: Run GoReleaser (arm64 only)
              uses: goreleaser/goreleaser-action@1a80836c5c9d9e5755a25cb59ec6f45a3b5f41a8 # v7
              with:
                  distribution: goreleaser
                  version: latest
                  args: release --clean --config .goreleaser-arm64.yaml ${{ github.event_name == 'workflow_dispatch' && github.ref_type != 'tag' && '--snapshot' || '' }}
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  GORELEASER_CURRENT_TAG: ${{ github.event.inputs.simulate_tag || github.ref_name }}

            - name: Upload arm64 artifacts
              uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
              with:
                  name: release-artifacts-arm64
                  path: |
                      dist/*.tar.gz
                      dist/*.zip
                  retention-days: 1

    release:
        name: Create Release
        runs-on: ubuntu-latest
        needs: [build-amd64, build-arm64]
        if: github.ref_type == 'tag'
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
              with:
                  fetch-depth: 0

            - name: Download amd64 artifacts
              uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
              with:
                  name: release-artifacts-amd64
                  path: dist/

            - name: Download arm64 artifacts
              uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
              with:
                  name: release-artifacts-arm64
                  path: dist/

            - name: Generate checksums
              run: |
                  cd dist
                  sha256sum *.tar.gz *.zip > checksums.txt
                  cat checksums.txt

            - name: Create GitHub Release
              uses: softprops/action-gh-release@b4309332981a82ec1c5618f44dd2e27cc8bfbfda # v3
              with:
                  files: |
                      dist/*.tar.gz
                      dist/*.zip
                      dist/checksums.txt
                  generate_release_notes: true
                  draft: false
                  prerelease: false
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    # =========================================================================
    # Packaging pipeline (new) — RPM + DEB build, sign, push, backup, manifest
    # =========================================================================

    determine-repo-type:
        name: Determine repo routing
        runs-on: ubuntu-latest
        # Runs when a real v* tag is pushed, or when workflow_dispatch was
        # invoked with simulate_tag set. The latter enables branch-side
        # end-to-end packaging tests without touching S3 / live repos.
        if: github.ref_type == 'tag' || github.event.inputs.simulate_tag != ''
        outputs:
            repo-type: ${{ steps.ctx.outputs.repo-type }}
            component-version: ${{ steps.ctx.outputs.component-version }}
            component-buildnum: ${{ steps.ctx.outputs.component-buildnum }}
            kb-db-release-tag: ${{ steps.ctx.outputs.kb-db-release-tag }}
            effective-tag: ${{ steps.ctx.outputs.effective-tag }}
            simulated: ${{ steps.ctx.outputs.simulated }}
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Parse tag and source pkg/common.sh
              id: ctx
              env:
                  SIMULATE_TAG: ${{ github.event.inputs.simulate_tag }}
              run: |
                  set -euo pipefail

                  # Pick the effective tag: simulate_tag input wins if non-empty,
                  # otherwise the real pushed tag. The if-guard above ensures
                  # at least one is set when we get here.
                  if [ -n "${SIMULATE_TAG}" ]; then
                      effective_tag="${SIMULATE_TAG}"
                      simulated=true
                      echo "::notice::Simulated tag mode — using ${effective_tag}"
                  else
                      effective_tag="${GITHUB_REF_NAME}"
                      simulated=false
                  fi

                  case "${effective_tag}" in
                      v*-test*|v*-dev*)
                          repo_type=daily
                          ;;
                      v*-rc*|v*-beta*|v*-alpha*|v*)
                          repo_type=staging
                          ;;
                      *)
                          echo "::error::Unsupported tag pattern: ${effective_tag}"
                          exit 1
                          ;;
                  esac

                  # Extract version + buildnum from the tag.
                  #   v1.0.0          -> cv=1.0.0      cbn=1
                  #   v1.0.0-beta1    -> cv=1.0.0      cbn=beta1_1
                  #   v0.0.1-test1    -> cv=0.0.1      cbn=test1_1
                  # The trailing "_1" is the package-iteration counter; bump
                  # COMPONENT_BUILDNUM manually if a second build of the same
                  # upstream tag is ever needed.
                  ref="${effective_tag#v}"
                  if [[ "${ref}" == *-* ]]; then
                      cv="${ref%%-*}"
                      cbn="${ref#*-}_1"
                  else
                      cv="${ref}"
                      cbn="1"
                  fi

                  # Source pkg/common.sh to recover KB_DB_RELEASE_TAG (the
                  # single source of truth for which kb.db release to bundle).
                  export COMPONENT_VERSION="${cv}"
                  export COMPONENT_BUILDNUM="${cbn}"
                  # shellcheck source=pkg/common.sh
                  source pkg/common.sh

                  echo "repo-type=${repo_type}"                 >> "$GITHUB_OUTPUT"
                  echo "component-version=${cv}"                >> "$GITHUB_OUTPUT"
                  echo "component-buildnum=${cbn}"              >> "$GITHUB_OUTPUT"
                  echo "kb-db-release-tag=${KB_DB_RELEASE_TAG}" >> "$GITHUB_OUTPUT"
                  echo "effective-tag=${effective_tag}"         >> "$GITHUB_OUTPUT"
                  echo "simulated=${simulated}"                 >> "$GITHUB_OUTPUT"

                  echo "Tag:                ${effective_tag}"
                  echo "Simulated:          ${simulated}"
                  echo "Repo type:          ${repo_type}"
                  echo "Component version:  ${cv}"
                  echo "Component buildnum: ${cbn}"
                  echo "KB DB release tag:  ${KB_DB_RELEASE_TAG}"
                  echo "PGEDGE_KB_VERSION:  ${PGEDGE_KB_VERSION}"
                  echo "PGEDGE_KB_BUILDNUM: ${PGEDGE_KB_BUILDNUM}"

    validate-kb-db:
        name: Validate kb.db
        runs-on: ubuntu-latest
        needs: determine-repo-type
        env:
            GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
            KB_TAG: ${{ needs.determine-repo-type.outputs.kb-db-release-tag }}
        steps:
            - name: Install sqlite3
              run: sudo apt-get update && sudo apt-get install -y sqlite3

            - name: Download kb.db from kb release
              run: |
                  set -euo pipefail
                  mkdir -p kb.db.cached
                  gh release download "${KB_TAG}" \
                      --repo "${GITHUB_REPOSITORY}" \
                      --pattern 'kb.db' \
                      --dir kb.db.cached
                  ls -lh kb.db.cached/

            - name: Validate schema, row counts, embedding dimensions
              run: |
                  set -euo pipefail
                  db=kb.db.cached/kb.db

                  size=$(stat -c%s "${db}" 2>/dev/null || stat -f%z "${db}")
                  echo "kb.db size: ${size} bytes"
                  if [ "${size}" -lt 10485760 ]; then
                      echo "::error::kb.db smaller than 10 MB (${size} bytes)"
                      exit 1
                  fi

                  tables=$(sqlite3 "${db}" "SELECT name FROM sqlite_master WHERE type='table' ORDER BY name;")
                  echo "Tables found:"; echo "${tables}" | sed 's/^/  /'
                  echo "${tables}" | grep -qx chunks       || { echo "::error::missing table chunks";       exit 1; }
                  echo "${tables}" | grep -qx source_files || { echo "::error::missing table source_files"; exit 1; }

                  chunk_count=$(sqlite3 "${db}" "SELECT COUNT(*) FROM chunks;")
                  echo "chunks count: ${chunk_count}"
                  if [ "${chunk_count}" -lt 1000 ]; then
                      echo "::error::chunk count ${chunk_count} below floor 1000"
                      exit 1
                  fi

                  src_count=$(sqlite3 "${db}" "SELECT COUNT(*) FROM source_files;")
                  echo "source_files count: ${src_count}"
                  if [ "${src_count}" -lt 1 ]; then
                      echo "::error::no source_files recorded"
                      exit 1
                  fi

                  # Embedding-dimension consistency: each provider column must
                  # have a single distinct BLOB length across all rows where
                  # set. Columns may be NULL for chunks not embedded by that
                  # provider — those rows are excluded from the check.
                  for col in openai_embedding voyage_embedding ollama_embedding; do
                      dims=$(sqlite3 "${db}" "SELECT DISTINCT length(${col}) FROM chunks WHERE ${col} IS NOT NULL;" || true)
                      if [ -z "${dims}" ]; then
                          echo "  ${col}: not populated (skipping)"
                          continue
                      fi
                      n_dims=$(echo "${dims}" | grep -c .)
                      if [ "${n_dims}" -gt 1 ]; then
                          echo "::error::inconsistent ${col} dimensions: ${dims}"
                          exit 1
                      fi
                      echo "  ${col}: dimension ${dims}"
                  done

                  echo "kb.db validation passed"

            - name: Upload validated kb.db
              uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
              with:
                  name: kb-db-validated
                  path: kb.db.cached/kb.db
                  retention-days: 7

    # -------------------------------------------------------------------------
    # RPM packaging
    # -------------------------------------------------------------------------

    package-rpm:
        name: Package RPM (${{ matrix.image }} ${{ matrix.arch }})
        needs: [determine-repo-type, validate-kb-db, build-amd64, build-arm64]
        runs-on: ${{ matrix.arch == 'arm64' && 'ubuntu-24.04-arm' || 'ubuntu-latest' }}
        strategy:
            fail-fast: false
            matrix:
                image: ['almalinux:9', 'almalinux:10']
                arch: [amd64, arm64]
        env:
            REPO_TYPE: ${{ needs.determine-repo-type.outputs.repo-type }}
            COMPONENT_VERSION: ${{ needs.determine-repo-type.outputs.component-version }}
            COMPONENT_BUILDNUM: ${{ needs.determine-repo-type.outputs.component-buildnum }}
            KB_TAG: ${{ needs.determine-repo-type.outputs.kb-db-release-tag }}
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Compute per-cell vars
              id: vars
              run: |
                  set -euo pipefail
                  case "${{ matrix.image }}" in
                      *:9)  os_version=9  ;;
                      *:10) os_version=10 ;;
                      *)    echo "::error::Unexpected image ${{ matrix.image }}"; exit 1 ;;
                  esac
                  case "${{ matrix.arch }}" in
                      amd64) tarball_arch=x86_64 ;;
                      arm64) tarball_arch=arm64  ;;
                  esac
                  echo "os-version=${os_version}"     >> "$GITHUB_OUTPUT"
                  echo "tarball-arch=${tarball_arch}" >> "$GITHUB_OUTPUT"
                  echo "timestamp=$(date -u +%Y%m%d-%H%M%S)" >> "$GITHUB_OUTPUT"

            - name: Download validated kb.db
              uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
              with:
                  name: kb-db-validated
                  path: pkg/kb.db.cached/

            - name: Download binary tarball for this arch
              uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
              with:
                  name: release-artifacts-${{ matrix.arch }}
                  path: pkg/binary.cached/

            - name: Filter binary cache to current-tag linux tarball
              env:
                  TARBALL_ARCH: ${{ steps.vars.outputs.tarball-arch }}
              run: |
                  set -euo pipefail
                  # Keep only the linux tarball for this arch; drop darwin /
                  # windows entries that came bundled in release-artifacts-*.
                  pattern="pgedge-ai-kb-builder_*_linux_${TARBALL_ARCH}.tar.gz"
                  find pkg/binary.cached/ -mindepth 1 -type f ! -name "${pattern}" -delete
                  shopt -s nullglob
                  matches=(pkg/binary.cached/${pattern})
                  shopt -u nullglob
                  if [ "${#matches[@]}" -ne 1 ]; then
                      echo "::error::Expected exactly one tarball matching ${pattern}, found ${#matches[@]}"
                      ls -la pkg/binary.cached/
                      exit 1
                  fi
                  echo "Staged binary tarball:"
                  ls -la pkg/binary.cached/

            - name: Checkout pgEdge action repos
              env:
                  TOKEN: ${{ secrets.PGEDGE_BUILDER_TOKEN }}
              run: |
                  set -euo pipefail
                  mkdir -p .github/actions
                  for repo in pgedge-builder-action pgedge-backup-artifacts pgedge-dnf-repo-builder; do
                      git clone --depth 1 \
                          "https://x-access-token:${TOKEN}@github.com/pgEdge/${repo}.git" \
                          ".github/actions/${repo}"
                  done

            - name: Build RPM
              uses: ./.github/actions/pgedge-builder-action
              with:
                  image: ${{ matrix.image }}
                  arch: ${{ matrix.arch }}
                  component_name: pkg
                  component_version: ${{ env.COMPONENT_VERSION }}
                  component_buildnum: ${{ env.COMPONENT_BUILDNUM }}
                  repo_type: ${{ env.REPO_TYPE }}
                  gpg_private_key: ${{ secrets.GPG_FIPS_RPM_PRIVATE_KEY }}
                  gpg_public_key: ${{ secrets.GPG_FIPS_RPM_PUBLIC_KEY }}
                  pgedge_builder_token: ${{ secrets.PGEDGE_BUILDER_TOKEN }}

            - name: Upload RPM cell artifact
              uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
              with:
                  name: rpm__el-${{ steps.vars.outputs.os-version }}__${{ matrix.arch }}
                  path: output/el${{ steps.vars.outputs.os-version }}-${{ matrix.arch }}/
                  retention-days: 7

    push-dnf:
        name: Push DNF (all EL majors, serial)
        # Strictly tag-only — branch tests (simulate_tag) never push to live repos.
        # force_push lets a maintainer publish from a partial build (some
        # cells failed). Without it, any failed cell skips the whole push.
        if: |
            always() && github.ref_type == 'tag' &&
            (needs.package-rpm.result == 'success' ||
             (github.event.inputs.force_push == 'true' && needs.package-rpm.result != 'cancelled'))
        needs: [determine-repo-type, package-rpm]
        runs-on: ubuntu-latest
        # Single job across all EL majors. Steps below invoke the dnf repo
        # builder once per major sequentially so two majors never write to S3
        # at the same time. Concurrency groups one push job per repo-type so
        # two simultaneous tag pushes serialize at the bucket boundary.
        concurrency:
            group: dnf-push-${{ needs.determine-repo-type.outputs.repo-type }}
            cancel-in-progress: false
        # Timestamp is the one passed to pgedge-backup-artifacts, so the
        # published manifest references the exact S3 prefix written.
        outputs:
            timestamp: ${{ steps.organize.outputs.timestamp }}
        env:
            REPO_TYPE: ${{ needs.determine-repo-type.outputs.repo-type }}
            COMPONENT_VERSION: ${{ needs.determine-repo-type.outputs.component-version }}
        steps:
            - name: Free disk space
              # Each RPM cell artifact is ~2GB (binary + src.rpm + kb.db).
              # 4 of them, extracted, then copied into rpms-el{9,10}/, easily
              # exceeds the runner's default ~14GB free space. Strip the
              # large pre-installed toolchains we don't use (.NET, Android,
              # Haskell, Java, CodeQL) to free ~25-30GB before download.
              run: |
                  set -eu
                  echo "Before cleanup:"
                  df -h /
                  sudo rm -rf \
                      /usr/share/dotnet \
                      /usr/local/lib/android \
                      /opt/ghc \
                      /opt/hostedtoolcache/CodeQL \
                      /usr/local/share/boost \
                      /usr/local/lib/node_modules
                  sudo apt-get clean
                  echo "After cleanup:"
                  df -h /

            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Download all RPM cell artifacts
              uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
              with:
                  pattern: rpm__el-*__*
                  path: rpm-artifacts
                  # Keep per-cell subdirs so we can identify each RPM's EL major.
                  merge-multiple: false

            - name: Organize RPMs by EL major
              id: organize
              run: |
                  set -euo pipefail
                  # mv (not cp) so we don't keep two copies of multi-GB RPMs
                  # on the runner's small disk. The rpm-artifacts/ tree is no
                  # longer needed after this step.
                  for os_version in 9 10; do
                      mkdir -p "rpms-el${os_version}"
                      shopt -s nullglob
                      for src_dir in rpm-artifacts/rpm__el-${os_version}__*; do
                          [ -d "${src_dir}" ] || continue
                          find "${src_dir}" -type f -name '*.rpm' -exec mv -v {} "rpms-el${os_version}/" \;
                      done
                      shopt -u nullglob
                      count=$(find "rpms-el${os_version}" -maxdepth 1 -name '*.rpm' | wc -l)
                      echo "EL${os_version}: ${count} RPMs"
                      echo "el${os_version}_count=${count}" >> "$GITHUB_OUTPUT"
                  done
                  # Drop the now-empty download dir to reclaim its space.
                  rm -rf rpm-artifacts
                  df -h /
                  # Single timestamp shared by all backups in this push job.
                  echo "timestamp=$(date -u +%Y%m%d-%H%M%S)" >> "$GITHUB_OUTPUT"

            - name: Checkout pgEdge action repos
              env:
                  TOKEN: ${{ secrets.PGEDGE_BUILDER_TOKEN }}
              run: |
                  set -euo pipefail
                  mkdir -p .github/actions
                  for repo in pgedge-dnf-repo-builder pgedge-backup-artifacts; do
                      git clone --depth 1 \
                          "https://x-access-token:${TOKEN}@github.com/pgEdge/${repo}.git" \
                          ".github/actions/${repo}"
                  done

            # ---------- EL 9 ----------
            - name: Push EL9 RPMs to yum repo
              if: steps.organize.outputs.el9_count != '0'
              uses: ./.github/actions/pgedge-dnf-repo-builder
              with:
                  s3-bucket: ${{ secrets.S3_BUCKET_NAME }}
                  cf-distribution: ${{ secrets.CLOUDFRONT_DISTRIBUTION }}
                  repo-type: ${{ env.REPO_TYPE }}
                  os-version: '9'
                  rpm-dir: rpms-el9
                  aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

            - name: Back up EL9 RPMs to S3
              if: steps.organize.outputs.el9_count != '0'
              uses: ./.github/actions/pgedge-backup-artifacts
              with:
                  s3-backup-bucket: ${{ secrets.S3_BACKUP_BUCKET_NAME }}
                  repo-type: ${{ env.REPO_TYPE }}
                  os-version: '9'
                  output-dir: rpms-el9
                  component-name: pgedge-ai-kb
                  component-version: ${{ env.COMPONENT_VERSION }}
                  timestamp: ${{ steps.organize.outputs.timestamp }}
                  aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

            # ---------- EL 10 ----------
            - name: Push EL10 RPMs to yum repo
              if: steps.organize.outputs.el10_count != '0'
              uses: ./.github/actions/pgedge-dnf-repo-builder
              with:
                  s3-bucket: ${{ secrets.S3_BUCKET_NAME }}
                  cf-distribution: ${{ secrets.CLOUDFRONT_DISTRIBUTION }}
                  repo-type: ${{ env.REPO_TYPE }}
                  os-version: '10'
                  rpm-dir: rpms-el10
                  aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

            - name: Back up EL10 RPMs to S3
              if: steps.organize.outputs.el10_count != '0'
              uses: ./.github/actions/pgedge-backup-artifacts
              with:
                  s3-backup-bucket: ${{ secrets.S3_BACKUP_BUCKET_NAME }}
                  repo-type: ${{ env.REPO_TYPE }}
                  os-version: '10'
                  output-dir: rpms-el10
                  component-name: pgedge-ai-kb
                  component-version: ${{ env.COMPONENT_VERSION }}
                  timestamp: ${{ steps.organize.outputs.timestamp }}
                  aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

            # No manifest writes here — publish-manifest reconstructs backup
            # paths from this job's timestamp output.

    # -------------------------------------------------------------------------
    # DEB packaging
    # -------------------------------------------------------------------------

    package-deb:
        name: Package DEB (${{ matrix.image }} ${{ matrix.arch }})
        needs: [determine-repo-type, validate-kb-db, build-amd64, build-arm64]
        runs-on: ${{ matrix.arch == 'arm64' && 'ubuntu-24.04-arm' || 'ubuntu-latest' }}
        strategy:
            fail-fast: false
            matrix:
                include:
                    - { image: 'ubuntu:jammy',    distro: jammy,    arch: amd64 }
                    - { image: 'ubuntu:jammy',    distro: jammy,    arch: arm64 }
                    - { image: 'ubuntu:noble',    distro: noble,    arch: amd64 }
                    - { image: 'ubuntu:noble',    distro: noble,    arch: arm64 }
                    - { image: 'debian:bullseye', distro: bullseye, arch: amd64 }
                    - { image: 'debian:bullseye', distro: bullseye, arch: arm64 }
                    - { image: 'debian:bookworm', distro: bookworm, arch: amd64 }
                    - { image: 'debian:bookworm', distro: bookworm, arch: arm64 }
                    - { image: 'debian:trixie',   distro: trixie,   arch: amd64 }
                    - { image: 'debian:trixie',   distro: trixie,   arch: arm64 }
        env:
            REPO_TYPE: ${{ needs.determine-repo-type.outputs.repo-type }}
            COMPONENT_VERSION: ${{ needs.determine-repo-type.outputs.component-version }}
            COMPONENT_BUILDNUM: ${{ needs.determine-repo-type.outputs.component-buildnum }}
            KB_TAG: ${{ needs.determine-repo-type.outputs.kb-db-release-tag }}
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Compute per-cell vars
              id: vars
              run: |
                  set -euo pipefail
                  case "${{ matrix.arch }}" in
                      amd64) tarball_arch=x86_64 ;;
                      arm64) tarball_arch=arm64  ;;
                  esac
                  echo "tarball-arch=${tarball_arch}"        >> "$GITHUB_OUTPUT"
                  echo "timestamp=$(date -u +%Y%m%d-%H%M%S)" >> "$GITHUB_OUTPUT"

            - name: Download validated kb.db
              uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
              with:
                  name: kb-db-validated
                  path: pkg/kb.db.cached/

            - name: Download binary tarball for this arch
              uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
              with:
                  name: release-artifacts-${{ matrix.arch }}
                  path: pkg/binary.cached/

            - name: Filter binary cache to current-tag linux tarball
              env:
                  TARBALL_ARCH: ${{ steps.vars.outputs.tarball-arch }}
              run: |
                  set -euo pipefail
                  pattern="pgedge-ai-kb-builder_*_linux_${TARBALL_ARCH}.tar.gz"
                  find pkg/binary.cached/ -mindepth 1 -type f ! -name "${pattern}" -delete
                  shopt -s nullglob
                  matches=(pkg/binary.cached/${pattern})
                  shopt -u nullglob
                  if [ "${#matches[@]}" -ne 1 ]; then
                      echo "::error::Expected exactly one tarball matching ${pattern}, found ${#matches[@]}"
                      ls -la pkg/binary.cached/
                      exit 1
                  fi
                  echo "Staged binary tarball:"
                  ls -la pkg/binary.cached/

            - name: Checkout pgEdge action repos
              env:
                  TOKEN: ${{ secrets.PGEDGE_BUILDER_TOKEN }}
              run: |
                  set -euo pipefail
                  mkdir -p .github/actions
                  for repo in pgedge-builder-action pgedge-backup-artifacts pgedge-apt-repo-builder; do
                      git clone --depth 1 \
                          "https://x-access-token:${TOKEN}@github.com/pgEdge/${repo}.git" \
                          ".github/actions/${repo}"
                  done

            - name: Build DEB
              uses: ./.github/actions/pgedge-builder-action
              with:
                  image: ${{ matrix.image }}
                  arch: ${{ matrix.arch }}
                  component_name: pkg
                  component_version: ${{ env.COMPONENT_VERSION }}
                  component_buildnum: ${{ env.COMPONENT_BUILDNUM }}
                  repo_type: ${{ env.REPO_TYPE }}
                  gpg_private_key: ${{ secrets.GPG_FIPS_DEB_PRIVATE_KEY }}
                  gpg_public_key: ${{ secrets.GPG_FIPS_DEB_PUBLIC_KEY }}
                  pgedge_builder_token: ${{ secrets.PGEDGE_BUILDER_TOKEN }}

            - name: Upload DEB cell artifact
              uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
              with:
                  name: deb__${{ matrix.distro }}__${{ matrix.arch }}
                  path: output/${{ matrix.distro }}-${{ matrix.arch }}/
                  retention-days: 7

    push-apt:
        name: Push APT (all distros, serial)
        # Strictly tag-only — branch tests (simulate_tag) never push to live repos.
        # force_push lets a maintainer publish from a partial build.
        if: |
            always() && github.ref_type == 'tag' &&
            (needs.package-deb.result == 'success' ||
             (github.event.inputs.force_push == 'true' && needs.package-deb.result != 'cancelled'))
        needs: [determine-repo-type, package-deb]
        runs-on: ubuntu-latest
        # Single job across all distros. Push + backup are invoked once per
        # distro sequentially below; no parallel writes to the apt S3 bucket.
        concurrency:
            group: apt-push-${{ needs.determine-repo-type.outputs.repo-type }}
            cancel-in-progress: false
        # Timestamp differs from push-dnf's; each push job picks its own on
        # entry, matching the unified workflow's per-family timestamp.
        outputs:
            timestamp: ${{ steps.organize.outputs.timestamp }}
        env:
            REPO_TYPE: ${{ needs.determine-repo-type.outputs.repo-type }}
            COMPONENT_VERSION: ${{ needs.determine-repo-type.outputs.component-version }}
        steps:
            - name: Free disk space
              # 10 DEB cell artifacts × ~1GB each saturate the runner's
              # ~14GB default free space. Strip pre-installed toolchains
              # we don't need.
              run: |
                  set -eu
                  echo "Before cleanup:"
                  df -h /
                  sudo rm -rf \
                      /usr/share/dotnet \
                      /usr/local/lib/android \
                      /opt/ghc \
                      /opt/hostedtoolcache/CodeQL \
                      /usr/local/share/boost \
                      /usr/local/lib/node_modules
                  sudo apt-get clean
                  echo "After cleanup:"
                  df -h /

            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Download all DEB cell artifacts
              uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
              with:
                  pattern: deb__*__*
                  path: deb-artifacts
                  merge-multiple: false

            - name: Organize DEBs by distro
              id: organize
              run: |
                  set -euo pipefail
                  # mv (not cp) so we don't keep two copies of multi-GB
                  # DEBs on the runner's small disk.
                  for distro in jammy noble bullseye bookworm trixie; do
                      mkdir -p "debs-${distro}"
                      shopt -s nullglob
                      for src_dir in deb-artifacts/deb__${distro}__*; do
                          [ -d "${src_dir}" ] || continue
                          find "${src_dir}" -type f -name '*.deb' -exec mv -v {} "debs-${distro}/" \;
                      done
                      shopt -u nullglob
                      count=$(find "debs-${distro}" -maxdepth 1 -name '*.deb' | wc -l)
                      echo "${distro}: ${count} DEBs"
                      echo "${distro}_count=${count}" >> "$GITHUB_OUTPUT"
                  done
                  rm -rf deb-artifacts
                  df -h /
                  echo "timestamp=$(date -u +%Y%m%d-%H%M%S)" >> "$GITHUB_OUTPUT"

            - name: Checkout pgEdge action repos
              env:
                  TOKEN: ${{ secrets.PGEDGE_BUILDER_TOKEN }}
              run: |
                  set -euo pipefail
                  mkdir -p .github/actions
                  for repo in pgedge-apt-repo-builder pgedge-backup-artifacts; do
                      git clone --depth 1 \
                          "https://x-access-token:${TOKEN}@github.com/pgEdge/${repo}.git" \
                          ".github/actions/${repo}"
                  done

            # ---------- jammy ----------
            - name: Push jammy DEBs to apt repo
              if: steps.organize.outputs.jammy_count != '0'
              uses: ./.github/actions/pgedge-apt-repo-builder
              with:
                  s3-bucket: ${{ secrets.APT_S3_BUCKET_NAME }}
                  cf-distribution: ${{ secrets.APT_CLOUDFRONT_DISTRIBUTION }}
                  repo-type: ${{ env.REPO_TYPE }}
                  os-name: 'ubuntu:jammy'
                  deb-dir: debs-jammy
                  gpg-private-key: ${{ secrets.GPG_FIPS_DEB_PRIVATE_KEY }}
                  aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

            - name: Back up jammy DEBs to S3
              if: steps.organize.outputs.jammy_count != '0'
              uses: ./.github/actions/pgedge-backup-artifacts
              with:
                  s3-backup-bucket: ${{ secrets.S3_BACKUP_BUCKET_NAME }}
                  repo-type: ${{ env.REPO_TYPE }}
                  os-version: jammy
                  output-dir: debs-jammy
                  component-name: pgedge-ai-kb
                  component-version: ${{ env.COMPONENT_VERSION }}
                  timestamp: ${{ steps.organize.outputs.timestamp }}
                  aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

            # ---------- noble ----------
            - name: Push noble DEBs to apt repo
              if: steps.organize.outputs.noble_count != '0'
              uses: ./.github/actions/pgedge-apt-repo-builder
              with:
                  s3-bucket: ${{ secrets.APT_S3_BUCKET_NAME }}
                  cf-distribution: ${{ secrets.APT_CLOUDFRONT_DISTRIBUTION }}
                  repo-type: ${{ env.REPO_TYPE }}
                  os-name: 'ubuntu:noble'
                  deb-dir: debs-noble
                  gpg-private-key: ${{ secrets.GPG_FIPS_DEB_PRIVATE_KEY }}
                  aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

            - name: Back up noble DEBs to S3
              if: steps.organize.outputs.noble_count != '0'
              uses: ./.github/actions/pgedge-backup-artifacts
              with:
                  s3-backup-bucket: ${{ secrets.S3_BACKUP_BUCKET_NAME }}
                  repo-type: ${{ env.REPO_TYPE }}
                  os-version: noble
                  output-dir: debs-noble
                  component-name: pgedge-ai-kb
                  component-version: ${{ env.COMPONENT_VERSION }}
                  timestamp: ${{ steps.organize.outputs.timestamp }}
                  aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

            # ---------- bullseye ----------
            - name: Push bullseye DEBs to apt repo
              if: steps.organize.outputs.bullseye_count != '0'
              uses: ./.github/actions/pgedge-apt-repo-builder
              with:
                  s3-bucket: ${{ secrets.APT_S3_BUCKET_NAME }}
                  cf-distribution: ${{ secrets.APT_CLOUDFRONT_DISTRIBUTION }}
                  repo-type: ${{ env.REPO_TYPE }}
                  os-name: 'debian:bullseye'
                  deb-dir: debs-bullseye
                  gpg-private-key: ${{ secrets.GPG_FIPS_DEB_PRIVATE_KEY }}
                  aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

            - name: Back up bullseye DEBs to S3
              if: steps.organize.outputs.bullseye_count != '0'
              uses: ./.github/actions/pgedge-backup-artifacts
              with:
                  s3-backup-bucket: ${{ secrets.S3_BACKUP_BUCKET_NAME }}
                  repo-type: ${{ env.REPO_TYPE }}
                  os-version: bullseye
                  output-dir: debs-bullseye
                  component-name: pgedge-ai-kb
                  component-version: ${{ env.COMPONENT_VERSION }}
                  timestamp: ${{ steps.organize.outputs.timestamp }}
                  aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

            # ---------- bookworm ----------
            - name: Push bookworm DEBs to apt repo
              if: steps.organize.outputs.bookworm_count != '0'
              uses: ./.github/actions/pgedge-apt-repo-builder
              with:
                  s3-bucket: ${{ secrets.APT_S3_BUCKET_NAME }}
                  cf-distribution: ${{ secrets.APT_CLOUDFRONT_DISTRIBUTION }}
                  repo-type: ${{ env.REPO_TYPE }}
                  os-name: 'debian:bookworm'
                  deb-dir: debs-bookworm
                  gpg-private-key: ${{ secrets.GPG_FIPS_DEB_PRIVATE_KEY }}
                  aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

            - name: Back up bookworm DEBs to S3
              if: steps.organize.outputs.bookworm_count != '0'
              uses: ./.github/actions/pgedge-backup-artifacts
              with:
                  s3-backup-bucket: ${{ secrets.S3_BACKUP_BUCKET_NAME }}
                  repo-type: ${{ env.REPO_TYPE }}
                  os-version: bookworm
                  output-dir: debs-bookworm
                  component-name: pgedge-ai-kb
                  component-version: ${{ env.COMPONENT_VERSION }}
                  timestamp: ${{ steps.organize.outputs.timestamp }}
                  aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

            # ---------- trixie ----------
            - name: Push trixie DEBs to apt repo
              if: steps.organize.outputs.trixie_count != '0'
              uses: ./.github/actions/pgedge-apt-repo-builder
              with:
                  s3-bucket: ${{ secrets.APT_S3_BUCKET_NAME }}
                  cf-distribution: ${{ secrets.APT_CLOUDFRONT_DISTRIBUTION }}
                  repo-type: ${{ env.REPO_TYPE }}
                  os-name: 'debian:trixie'
                  deb-dir: debs-trixie
                  gpg-private-key: ${{ secrets.GPG_FIPS_DEB_PRIVATE_KEY }}
                  aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

            - name: Back up trixie DEBs to S3
              if: steps.organize.outputs.trixie_count != '0'
              uses: ./.github/actions/pgedge-backup-artifacts
              with:
                  s3-backup-bucket: ${{ secrets.S3_BACKUP_BUCKET_NAME }}
                  repo-type: ${{ env.REPO_TYPE }}
                  os-version: trixie
                  output-dir: debs-trixie
                  component-name: pgedge-ai-kb
                  component-version: ${{ env.COMPONENT_VERSION }}
                  timestamp: ${{ steps.organize.outputs.timestamp }}
                  aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

            # No manifest writes here — publish-manifest reconstructs backup
            # paths from this job's timestamp output.

    # -------------------------------------------------------------------------
    # Aggregated manifest for the future promotion tool
    # -------------------------------------------------------------------------

    publish-manifest:
        name: Publish aggregated backup manifest
        needs: [determine-repo-type, push-dnf, push-apt]
        if: always() && needs.determine-repo-type.result == 'success'
        runs-on: ubuntu-latest
        steps:
            - name: Install jq + awscli
              run: |
                  sudo apt-get update
                  sudo apt-get install -y python3-pip jq
                  pip3 install --user awscli
                  echo "$HOME/.local/bin" >> "$GITHUB_PATH"

            - name: Build path-only manifest
              env:
                  REPO_TYPE:        ${{ needs.determine-repo-type.outputs.repo-type }}
                  CV:               ${{ needs.determine-repo-type.outputs.component-version }}
                  CBN:              ${{ needs.determine-repo-type.outputs.component-buildnum }}
                  KB_TAG:           ${{ needs.determine-repo-type.outputs.kb-db-release-tag }}
                  EFFECTIVE_TAG:    ${{ needs.determine-repo-type.outputs.effective-tag }}
                  SIMULATED:        ${{ needs.determine-repo-type.outputs.simulated }}
                  PUSH_DNF_RESULT:  ${{ needs.push-dnf.result }}
                  PUSH_APT_RESULT:  ${{ needs.push-apt.result }}
                  DNF_TS:           ${{ needs.push-dnf.outputs.timestamp }}
                  APT_TS:           ${{ needs.push-apt.outputs.timestamp }}
                  S3_BACKUP_BUCKET: ${{ secrets.S3_BACKUP_BUCKET_NAME }}
              run: |
                  set -euo pipefail
                  mkdir -p aggregated

                  # Reconstruct backup paths from each push job's timestamp.
                  # We unconditionally list every matrix target — under
                  # force_push partial failure, a listed prefix may be empty
                  # in S3 and the promoter handles that via `aws s3 ls`.
                  dnf_backups='[]'
                  if [ -n "${DNF_TS}" ]; then
                      for os in 9 10; do
                          path="s3://${S3_BACKUP_BUCKET}/${REPO_TYPE}/pgedge-ai-kb/${CV}/${DNF_TS}/dnf/${os}/"
                          dnf_backups="$(jq -c --arg p "${path}" '. + [$p]' <<<"${dnf_backups}")"
                      done
                  fi

                  apt_backups='[]'
                  if [ -n "${APT_TS}" ]; then
                      for distro in jammy noble bullseye bookworm trixie; do
                          path="s3://${S3_BACKUP_BUCKET}/${REPO_TYPE}/pgedge-ai-kb/${CV}/${APT_TS}/apt/${distro}/"
                          apt_backups="$(jq -c --arg p "${path}" '. + [$p]' <<<"${apt_backups}")"
                      done
                  fi

                  jq -n \
                      --arg     run_id              "${GITHUB_RUN_ID}" \
                      --arg     run_attempt         "${GITHUB_RUN_ATTEMPT}" \
                      --arg     repo                "${GITHUB_REPOSITORY}" \
                      --arg     tag                 "${EFFECTIVE_TAG}" \
                      --argjson simulated           "${SIMULATED}" \
                      --arg     commit_sha          "${GITHUB_SHA}" \
                      --arg     component_name      "pgedge-ai-kb" \
                      --arg     component_version   "${CV}" \
                      --arg     component_buildnum  "${CBN}" \
                      --arg     kb_db_release_tag   "${KB_TAG}" \
                      --arg     repo_type           "${REPO_TYPE}" \
                      --arg     dnf_timestamp       "${DNF_TS}" \
                      --arg     apt_timestamp       "${APT_TS}" \
                      --arg     push_dnf_outcome    "${PUSH_DNF_RESULT}" \
                      --arg     push_apt_outcome    "${PUSH_APT_RESULT}" \
                      --argjson dnf_backups         "${dnf_backups}" \
                      --argjson apt_backups         "${apt_backups}" \
                      '{run_id:$run_id, run_attempt:$run_attempt,
                        repo:$repo, tag:$tag, simulated:$simulated,
                        commit_sha:$commit_sha,
                        component_name:$component_name,
                        component_version:$component_version,
                        component_buildnum:$component_buildnum,
                        kb_db_release_tag:$kb_db_release_tag,
                        repo_type:$repo_type,
                        timestamps:{dnf:$dnf_timestamp, apt:$apt_timestamp},
                        push_results:{dnf:$push_dnf_outcome, apt:$push_apt_outcome},
                        backups:{dnf:$dnf_backups, apt:$apt_backups}}' \
                      > aggregated/manifest.json
                  cat aggregated/manifest.json

            - name: Write step summary
              run: |
                  set -euo pipefail
                  {
                      echo "## Run \`${GITHUB_RUN_ID}\` attempt ${GITHUB_RUN_ATTEMPT}"
                      echo ""
                      jq -r '
                          "**Tag:** `\(.tag)`" + (if .simulated then " *(simulated)*" else "" end) + "  ",
                          "**Repo type:** `\(.repo_type)`  ",
                          "**Component:** `\(.component_name) \(.component_version)-\(.component_buildnum)`  ",
                          "**KB DB:** `\(.kb_db_release_tag)`  ",
                          "**Push results:** dnf=`\(.push_results.dnf)`, apt=`\(.push_results.apt)`  ",
                          "**Timestamps:** dnf=`\(.timestamps.dnf // "—")`, apt=`\(.timestamps.apt // "—")`"
                      ' aggregated/manifest.json
                      echo ""
                      echo "### Backup paths (DNF)"
                      jq -r '.backups.dnf[]? | "- `" + . + "`"' aggregated/manifest.json
                      if [ "$(jq -r '.backups.dnf | length' aggregated/manifest.json)" = "0" ]; then
                          echo "_(none — push-dnf skipped or no packages)_"
                      fi
                      echo ""
                      echo "### Backup paths (APT)"
                      jq -r '.backups.apt[]? | "- `" + . + "`"' aggregated/manifest.json
                      if [ "$(jq -r '.backups.apt | length' aggregated/manifest.json)" = "0" ]; then
                          echo "_(none — push-apt skipped or no packages)_"
                      fi
                  } >> "$GITHUB_STEP_SUMMARY"

            - name: Upload aggregated manifest to S3
              # Skipped on simulate_tag — the manifest still lands as a
              # workflow artifact below for inspection.
              if: github.ref_type == 'tag'
              env:
                  AWS_ACCESS_KEY_ID:     ${{ secrets.AWS_ACCESS_KEY_ID }}
                  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
                  S3_BACKUP_BUCKET:      ${{ secrets.S3_BACKUP_BUCKET_NAME }}
              run: |
                  set -euo pipefail
                  key="_runs/${GITHUB_RUN_ID}-r${GITHUB_RUN_ATTEMPT}.json"
                  aws s3 cp aggregated/manifest.json "s3://${S3_BACKUP_BUCKET}/${key}"
                  echo "Aggregated manifest uploaded to s3://${S3_BACKUP_BUCKET}/${key}"

            - name: Upload aggregated manifest as workflow artifact
              uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
              with:
                  name: aggregated-manifest
                  path: aggregated/manifest.json
                  retention-days: 90
